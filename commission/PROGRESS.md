# 현재 진행 상황

## 최근 완료한 작업
- 2026-09-02 (6차) — 사장님 선택으로 B "게시판" 으로 갈아탔다. 흰 바탕, 검정 3px 테두리, 딱딱한 그림자(6px/3px), 형광 노랑(#f6ef4f),
  검은고딕(Black Han Sans) 제목, 기울여 붙인 스티커, 머리 아래 검정 전광판(열린 메뉴와 남은 자리를 흘림). 낱말도 갈았다 —
  메뉴판 / 메뉴 붙이기·내리기 / 내 것. 피치 버튼 위 흰 글자는 디스플레이 20px 이라 대비 3.75 로 충분(굵은 18px 이상 기준 3.0).
  작은 붉은 글자는 #c0361f(5.5). A "작업실" 은 git 이력(aa9da7a)과 캔버스에 남아 있다.
- 2026-09-02 (5차) — 새 디자인 위에서 채점 3회차 **통과**(높음 0·중간 0·낮음 5). 낮음 5건 처리: "부터" 갈라짐, 채움색 위 회색 글자, 404 옛말,
  "작업실" 한 낱말이 셋을 가리키던 것(내 페이지로 분리), 고정 막대로 내려갔을 때 폼 머리가 가리던 것.
- 2026-09-02 (4차) — "틀을 깨는 디자인" 요청. 세 방향(A 작업실·B 게시판·C 스케치북)을 디자인 캔버스에 그려 비교하게 하고,
  추천안 A "작업실" 을 앱에 입혔다. 종이색 바탕(#f4efe7), 먹색 글자, 명조 제목(Gowun Batang, Google Fonts), 가는 괘선,
  기울여 찍은 "N자리 남음" 도장, 4:5 세로 사진, 좁은 화면 상세의 아래 고정 [의뢰하기] 막대. 공통 모양은 `app/ui.ts`.
  낱말도 방향에 맞췄다: 마이페이지 → "내 작업실", 커미션 열기 → "작업실 열기". 대비는 전부 재서 작은 글자 4.5 이상.
- 2026-09-02 (3차) — 별도 채점자(처음 쓰는 사람의 눈, `uiux/CLAUDE.md`)를 두고 유려하게 쓸 수 있는지 검사했다.
  1회차 미통과(중간 6·낮음 4) → 10건 처리 → 2회차 **통과**(높음 0·중간 0·낮음 3). 낮음 3건도 처리했다.
  기록은 `uiux/PROGRESS.md`. 큰 변화: 수락·완료에 확인 단계, 좁은 화면 탭 줄, 작은 글자 대비 4.5 이상, 낱말 통일.
- 2026-09-02 (2차) — 디자인을 텀블벅에서 따왔다. 색·글꼴은 dropbid 가 텀블벅 Figma 라이브러리에서 읽어 둔 실제 토큰,
  구조는 텀블벅 화면 그대로: 흰 바탕과 1px 구분선의 GNB(폭 1200), 4:3 썸네일에 그림자 없는 프로젝트 카드(4열/2열),
  상세는 왼쪽 스토리 + 오른쪽 고정 패널(큰 금액 → 피치색 CTA), 리워드 카드 모양의 테두리 상자, 알약 버튼.
  공통 모양은 `app/ui.ts` 한 곳에 있다. 모바일·데스크톱 스크린샷으로 5개 화면을 확인했다.
- 2026-09-02 — 프로젝트 생성부터 의뢰 → 수락 → 전달 → 완료 흐름까지 한 번에. `TASKS.md` [기반]·[데이터] 전부, [기능]·[화면] 대부분.
  - DB 테스트 22건 + 형식 테스트 4건 통과. 동시 수락 인수 조건 포함.
  - 브라우저(Playwright, 420px)로 시연 데이터 위에서 전 흐름을 실제로 밟았다: 의뢰인이 의뢰(10자 미만 거부 확인) → 창작자가 최종가 170,000원으로 수락 → 빈 전달 거부 → 메모로 전달 → 의뢰인 완료 확정 → 제3자는 의뢰 상세를 못 봄 → 새 창작자가 커미션 열고 닫으면 목록에서 사라짐.

## 다음에 진행할 작업
- [화면] 화면 QA 1회차 — 실제 기기에서 눈으로 보기. 그 뒤 [마무리] 배포.

## 확인하지 못한 것
- 명조(Gowun Batang)는 이 환경에서 Google Fonts 가 막혀 헤드리스 캡처에는 기기 글꼴로 나왔다. 실제 브라우저에서 명조로 보이는지 한 번 볼 것.
- 텀블벅 실제 사이트와 Figma 를 이번 세션에서 직접 열어 보지는 못했다 (사이트는 네트워크 정책, Figma 는 호출 한도).
  토큰은 라이브러리에서 읽은 값이 맞지만, 구조는 기억에 의존했다. 실제 화면과 나란히 놓고 한 번 맞춰 볼 것.
- **실제 기기에서 본 적이 없다.** 헤드리스 브라우저 스크린샷만 봤다.
- 갓 클론한 상태(`rm -rf node_modules .next && npm ci`)에서의 검증 3종 — CI 가 첫 푸시에서 확인한다.
- Supabase 실제 프로젝트에 마이그레이션이 그대로 올라가는지 — 로컬 Postgres 16 에서만 돌렸다. `bootstrap.sql` 이 `auth.users` 와 역할을 흉내내므로 Supabase 에서는 그 파일 없이 `supabase/migrations` 만 적용한다.
- 카카오 로그인·Storage·알림은 손대지 않았다.

## 알고 있는 문제
- 피치색 버튼 위 흰 글자는 대비가 3.4 라 작은 글자 기준(4.5)에는 못 미친다. 텀블벅 실제 버튼과 같은 선택이라 따랐고,
  그래서 굵은 16px 이상에서만 쓴다 (`app/ui.ts`). 접근성 기준을 올리려면 버튼색을 `--color-accent-deep` 보다 더 어둡게 잡아야 한다.
- 모바일 상세에서 의뢰 패널이 맨 아래에 있다. 텀블벅 모바일은 "후원하기" 를 화면 아래에 고정한다 — 같은 장치를 붙일지는 실제 기기 QA 뒤에.
- 채점자가 근거 없이 기록만 한 것: 의뢰도 넣고 커미션도 연 사람은 커미션을 여는 순간 "내가 넣은 의뢰" 칸이 맨 위에서 맨 아래로 이동한다. 탭 줄에 현재 탭 표시가 없다. 의뢰 취소에는 확인이 없다(수락 전이라 되돌릴 수 있는 일이라 뺐다).
- 없는 id 로 들어오면 "찾을 수 없는 페이지" 가 보이지만 HTTP 상태는 200 이다. `loading.tsx` 때문에 껍데기가 먼저 나가고 404 는 스트리밍으로 뒤에 온다. 사람 눈에는 문제없고 dropbid 도 같다. 검색엔진 대응이 필요해지면 그때 본다.
- 마감일이 지난 `accepted` 의뢰는 "N일 지남" 으로 표시만 하고 아무것도 하지 않는다. 정책이 정해지지 않았다.
- 커미션을 열고 나면 가격·기간을 못 바꾼다 (열기/닫기만). 바꾸려면 새로 연다.

## 다음 사람이 헷갈릴 만한 것
- **로그인이 없다.** 화면 위 "로그인 대신" 전환기로 사람을 바꿔 가며 창작자·의뢰인 양쪽을 본다. 시연 데이터의 김창작·이글꼴이 창작자, 박덕후·최의뢰가 의뢰인이다 (누구나 커미션을 열 수는 있다).
- 슬롯은 `accepted` + `delivered` 만 센다. `requested` 는 안 센다 — 대기 의뢰가 아무리 쌓여도 창작자가 고를 수 있어야 한다. 전달만 해서는 자리가 안 비고 **의뢰인이 완료를 눌러야** 빈다 (테스트로 고정).
- 의뢰 상태는 `requests` 에 직접 쓰지 않고 DB 함수로만 바꾼다 (`CLAUDE.md` 규칙 목록). 권한으로도 막혀 있다.
- `npm test` 는 로컬 DB 를 통째로 지우고 다시 만든다. 화면을 보려면 그 뒤 `npm run db:seed`.
- 시연 데이터에서 박덕후는 로고 커미션에 이미 대기 의뢰가 있다. 같은 커미션에 또 넣으면 "이미 수락을 기다리는 의뢰가 있습니다" 가 뜨는데, 그건 의도된 동작이다 (브라우저 검증 중 한 번 헷갈렸다).
