# 할 일 목록

한 항목은 30분 이내에 끝낼 크기로 유지한다.
"~시스템 구현", "~전체 완성" 같은 항목이 보이면 그 자리에서 쪼갠다.
위에서부터 순서대로 진행한다.

[규칙] 항목은 `specs/battle.md`, [화면] 항목은 `specs/screens.md` 를 읽는다.
[규칙] 항목은 해당 규칙의 테스트를 `tests/` 에 **같이** 추가해야 완료다.

## [기반]
- [x] `index.html` 뼈대 — 빈 `<script id="core">`(전역 `Core` 객체) + 빈 화면 스크립트, 어두운 배경. `tests/html.test.mjs` 로 ① 모든 script 문법 검사 ② 외부 주소(`src`/`href` 의 http·//) 0개 ③ core 를 Node 로 불러 `Core` 가 있는지 검사

## [규칙]
- [x] 맵 데이터 — 8×8 지형, 칸별 통과 가능·서낭당·약수터 조회 함수 + 테스트
- [x] 유닛 데이터 — 아군 5·요괴 6의 수치·칭호·시작 위치, 새 전투 상태를 만드는 `Core.newBattle()` + 테스트 (기력 6, 1턴, 흐림, 공적 0)
- [x] 이동 가능 칸 계산 — 이동력·막힌 칸·유닛 칸 통과 불가, 제자리 포함 + 테스트
- [x] 거리와 대상 찾기 — 공격·스킬 대상(적), 생명수 대상(거리 2 안 아군, 자기 포함) + 테스트
- [x] 오행 상성 배율 — ×1.5 / ×0.7 / ×1, '없음' 처리 + 테스트
- [x] 피해 계산 — 기본공격·스킬·날씨·서낭당·마지막 반올림. `specs/battle.md` 7장 검산 예시 5개를 테스트로 + 날씨 3종 테스트
- [x] 행동 실행 — 이동·공격·스킬(기력 −4)·생명수(최대 HP 초과 안 함)·대기·취소, 행동 완료 표시 + 테스트
- [x] HP 0 처리와 공적 — 요괴 제거, 아군 퇴각, 벽사청 일격 +1/흑린 +3, 객장 0 + 테스트
- [x] 승패 판정 — 흑린 격파 즉시 승리, 전원 퇴각 패배, 10턴 요괴 페이즈 후 패배 + 테스트
- [x] 턴 진행 — 아군 페이즈 시작(날씨 4·7·10턴, 약수터, 기력 +1 2턴부터), 요괴 페이즈 시작(약수터), 턴 +1. 날씨 랜덤은 인자로 받음 + 테스트
- [x] 일반 요괴 AI — 공격 가능 시 HP 최저 대상, 동률 규칙, 공격 칸 선택, 불가 시 접근 이동, 길 없을 때 제자리 + 테스트
- [x] 흑린 AI — 이동 없음, 인접 아군 중 HP 최저 공격 + 테스트
- [x] 요괴 페이즈 한 번 실행 — 표 순서대로 행동, 도중 전원 퇴각 시 즉시 패배 + 테스트
- [x] 전투 끝까지 돌려보기 테스트 — 아군이 매 턴 대기만 하면 패배로 끝남, 고정 시나리오로 흑린을 잡으면 승리로 끝남 (무한 반복 없음 확인)

## [화면]
- [x] 맵 그리기 — 색칸 지형, 유닛 이모지, 세력 테두리(붉은·회색+객장·보라), 360px 세로에 맵 전체
- [x] 위쪽 줄 — 턴 N/10 · 날씨 · 공적 N
- [x] 유닛 선택과 이동 — 탭 → 파란 칸 → 이동, 선택 해제. 터치·클릭 둘 다
- [x] 행동 메뉴 — 공격/스킬/대기/취소, 비활성 조건(대상 없음·기력 부족), 행동 끝난 유닛 회색
- [x] 대상 고르기 — 빨간/초록 칸, 스킬 대상 위 "유리!"/"불리", 피해·회복 숫자 떠올랐다 사라짐
- [x] 정보창 — 이름/소속/칭호/HP/기력/속성, 요괴 탭 시 정보만
- [x] 턴 종료와 요괴 페이즈 연출 — [턴 종료], 전원 행동 시 자동, 요괴 0.5초 간격, 그동안 입력 잠금
- [x] 대사 장면 — 초상화+말풍선, 탭으로 넘김, 시작 대사 5줄 후 전투 시작
- [x] 승리 후 대사와 승패 화면 — 달래 사라짐 연출, 승리/패배 문구, [다시하기]로 시작 대사부터 초기 상태

## [v0.2 구조]
- [x] 스테이지 데이터 분리 — 1장 맵·크기·배치를 `Core.STAGES.ch1` 로, `newBattle(stage)`, 지형 함수에 state 인자, 화면은 `state.rows/cols` 로 그림. 규칙 동작은 그대로 (기존 테스트는 호출 모양만 바꿔 전부 통과)

## [v0.2 연출] (`specs/effects.md`)
- [x] 이동 경로 — 이동 이벤트에 `path`(지나간 칸), `Core.pathTo` + 테스트
- [x] 이동 미끄러짐과 요괴 발자국 — 칸당 0.08초, 보라 점 0.6초, 연출 중 입력 잠금
- [x] 피격 연출 — 번쩍·흔들림, 유리 숫자 1.4배·불리 숫자 작게 (이벤트에 상성 배율 `mult` 추가 + 테스트)
- [x] 쓰러짐 연출 — 0.4초 흐려지며 사라짐, 동작 줄이기 설정 존중

## [v0.2 에디터] (`specs/editor.md`)
- [x] 스테이지 검사와 맵 코드 — `Core.validateStage` / `encodeStage` / `decodeStage` + 테스트
- [x] 편집 규칙 — `Core.paintStage(stage, tool, r, c)` (지형·아군 1명씩·흑린 1마리·초가집 위 금지·지우개), `Core.resizeStage` + 테스트
- [x] 에디터 화면 — `#editor`·[🛠 맵 에디터] 버튼으로 진입, 맵 그리기, 크기 조절, [게임으로]
- [x] 도구 줄과 칠하기 — 지형 4·아군 5·요괴 4·지우개, 지형 끌어 칠하기(터치·마우스), 경고 표시
- [x] 코드 복사·붙여넣기 — 복사 실패 시 코드 칸 선택, 잘못된 코드 오류 문구
- [x] 브라우저 저장 — 이름으로 저장·불러오기·삭제, 저장 불가 시 안내

## [v0.3 규칙] (`specs/chapter2.md`)
- [x] 보스 일반화 — 승패·에디터 검사를 "흑린"이 아닌 보스 기준으로, `Core.maxMerit(stage)` (보스 아닌 적 수 + 3) + 테스트
- [x] 2장 유닛 정의 — 관군(아군), 신당회 무녀·정화된 도깨비·정화된 장산범·달래(보스) 수치·소속·칭호, 달래 기력 + 테스트
- [x] 2장 임시 스테이지 — `Core.STAGES.ch2` (10×8, 12턴, 보충 S8), 검사 통과 + 테스트
- [x] 품계와 관군 — `Core.rankFor(누계)`, `newBattle(stage, { merit })` 로 관군 0~2명 배치(S1)·윤무겸 칭호에 품계(S12) + 테스트
- [ ] 적 기력 — 기력 있는 적은 2턴부터 요괴 페이즈 시작에 +1 + 테스트
- [ ] 무녀 AI — 거리 2 에서 쏘기(S5) + 테스트
- [ ] 달래 AI — 도망·생명수(S3·S4), 공격 안 함 + 테스트
- [ ] 진행 계산 — `Core.applyResult(progress, stageId, result, merit)`·다음 장 결정(S7·S9) + 테스트
- [ ] 2장 끝까지 돌려보기 — 대기만 하면 패배, 무한 반복 없음, 자동 플레이어 결과를 PROGRESS 에 기록

## [v0.3 화면]
- [ ] 첫 화면 — [처음부터]/[이어하기], 공적 누계·품계 표시, 브라우저 저장(막히면 안내)
- [ ] 장 흐름 — 1장 승리 → [다음 장으로], 장별 대사·결과 문구·턴 수, 2장 승리 연출(달래 도주·🔮 남김), 2장 뒤 "준비 중" 화면
- [ ] 2장 유닛 표시 — 관군·신당회 유닛 테두리·정보창(소속·칭호·달래 기력), 적 생명수 초록 숫자
